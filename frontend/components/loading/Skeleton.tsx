'use client';

/**
 * Re-export domain skeletons for a single import path (`@/components/loading`).
 * Prefer `SkeletonLoader` variants for rich layouts.
 */
export {
  SkeletonLoader,
  type SkeletonLoaderProps,
} from '@/components/ui/SkeletonLoader';
